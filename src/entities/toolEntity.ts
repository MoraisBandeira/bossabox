class ToolEntity {
    constructor({ id, title, link, description, tags }) {
      this.id = id;
      this.title = title;
      this.link = link;
      this.description = description;
      this.tags = tags;
    }
  }
  
  export default ToolEntity;